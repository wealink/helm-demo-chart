apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-demo-chart.fullname" . }}
  labels:
    {{- include "helm-demo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: api
      protocol: TCP
      name: api
  selector:
    {{- include "helm-demo-chart.selectorLabels" . | nindent 4 }}
